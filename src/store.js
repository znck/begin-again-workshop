import Vue from 'vue'
import Vuex, { Store } from 'vuex'

Vue.use(Vuex)

const getters = {}
const state = () => ({})
const mutations = {}
const actions = {}

export default new Store({
  getters,
  state,
  mutations,
  actions
})
